import React, {Fragment} from "react";

//view
import View from "./view";

export const UrlParametersArea = (props) =>{
    return(
        <Fragment>
            <View {...props}/>
        </Fragment>
    );
}